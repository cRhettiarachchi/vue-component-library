---
"@titantech/vue-components": minor
---

Add Tabs, Tooltip, Popover, Pagination and Toast components, plus a public
composables layer (`useId`, `useClickOutside`, `useFocusTrap`, `useDebounceFn`,
`usePagination`, `useToast`) that the new overlay and list components are built
on. Adds `--ui-warning` and `--ui-danger` status tokens.
